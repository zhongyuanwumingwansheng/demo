package com.example.demo.service;

public class StringService {
    public int findSubStrLocation(String str, String subStr){
        //
        if(null==str||str.length()==0||null==subStr||subStr.length()==0){
            return -1;
        }

        return 0;

    }
}
